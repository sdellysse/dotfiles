#!/bin/sh

emacs_server() {
    local op="$1"

    case "$op" in
	start)
	    exec emacs --daemon
	    ;;

	stop)
	    exec emacsclient -e "(kill-emacs)"
	    ;;

	restart)
	    exec emacsclient -e "(kill-emacs)"
	    exec emacs --daemon
	    ;;
    esac
}

emacs_server "$1"
