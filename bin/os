#!/bin/sh

OS=$(uname -s)
if [[ $OS =~ 'CYGWIN' ]]; then
    echo "Windows"
elif [[ $OS =~ 'Linux' ]]; then
    echo "Linux"
elif [[ $OS =~ 'Darwin' ]]; then
    echo "OSX"
elif [[ $OS =~ 'SunOS' ]]; then
    echo "Solaris"
else
    echo "UNKNOWN:${OS}"
fi
