#!/bin/bash

OS=$(uname -s)
if [[ $OS =~ 'CYGWIN' ]]; then
    echo "Windows"
elif [[ $OS =~ 'Linux' ]]; then
    echo "Linux"
elif [[ $OS =~ 'Darwin' ]]; then
    echo "OSX"
else
    echo "UNKNOWN"
fi
