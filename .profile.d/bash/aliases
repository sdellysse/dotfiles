# vim: ft=sh

if [ $(os) != 'OSX' ]; then
    alias ls="/bin/ls --color=always -F"
else
    alias ls="/bin/ls -GF"
fi
alias l="ls -l"
alias la="l -a"
alias s='screen -RD'
alias -- -='cd -'
alias vssh='vagrant ssh'

touch $HOME/opt/.profile.d/bash/aliases && source $HOME/opt/.profile.d/bash/aliases
